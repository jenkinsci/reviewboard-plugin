<!--
  This view is used to render the plugin list page.

  Since we don't really have anything dynamic here, let's just use static HTML. 
-->
<div>
  Publishes SCM changes to Reviewboard code review system. Currently only works with Perforce, so the Perforce SCM plugin must also be installed.
</div>